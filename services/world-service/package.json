{
  "name": "world-service",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "sst test",
    "start": "sst start",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove"
  },
  "eslintConfig": {
    "extends": [
      "serverless-stack"
    ]
  },
  "dependencies": {
    "@aws-sdk/client-sns": "^3.47.0",
    "@serverless-stack/cli": "0.59.1",
    "@serverless-stack/resources": "0.59.1",
    "aws-cdk-lib": "2.7.0",
    "vrchat": "^1.6.5"
  }
}
